name: lock_pass_app_stacked
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_spinkit: ^5.2.0
  local_auth: ^2.2.0
  logger: ^1.4.0
  pixelarticons: ^0.4.0
  stacked: ^3.4.0
  stacked_services: ^1.1.0

dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  mockito: ^5.4.1
  stacked_generator: ^1.3.3
  flutter_launcher_icons: "^0.13.1"

flutter_icons:
    android: "launcher_icon"
    ios: true
    image_path: "assets/icon/icon.png"

flutter:
  assets:
    - assets/images/
  uses-material-design: true

